{
  "name": "ts-string-literal-enum-plugin",
  "version": "0.0.1",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "repository": "git@github.com:HearTao/ts-string-literal-enum-plugin.git",
  "author": "kingwl <kingwenlu@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.2.1",
    "typescript": "^4.2.2"
  },
  "scripts": {
    "build": "tsc",
    "bootstrap": "yarn && cd tests/demo && yarn --no-lockfile",
    "build:link": "yarn build && yarn link && cd tests/demo && yarn link ts-string-literal-enum-plugin",
    "prettier": "prettier -c --config ./prettier.config.js ./src/**/*.{ts,tsx} ./tests/**/*.{ts,tsx} --write"
  },
  "files": [
    "dist"
  ]
}
